# Expires map
map $sent_http_content_type $expires {
    default                    off;
    text/html                  epoch;
    text/css                   max;
    application/json           max;
    application/javascript     max;
    ~image/                    max;
}

server {
    listen 80;
    
    # Handle static assets first - must be before other locations
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|map)$ {
        root /usr/share/nginx/html;
        expires max;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
        access_log off;
    }
    
    # Handle assets directory specifically
    location /admin/assets/ {
        alias /usr/share/nginx/html/assets/;
        expires max;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
        access_log off;
    }
    
    # Internal app - /admin path (for HTML routes)
    location /admin {
        alias /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /admin/index.html;
        
        # Add headers for SPA
        add_header X-Frame-Options "SAMEORIGIN" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
    }
    
    # Handle root path - redirect to /admin
    location = / {
        return 301 /admin;
    }
    
    # Handle all other paths - redirect to /admin
    location / {
        return 301 /admin;
    }
    
    expires $expires;
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
