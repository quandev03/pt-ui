import { ActionType } from '@react/constants/app';
import ChangeSimmPage from '../FormAddEdit';

const enterpriseHistoryDetail = () => {
  return <ChangeSimmPage typeModal={ActionType.VIEW_ENTERPRISE_HISTORY} />;
};
export default enterpriseHistoryDetail;
