export enum TypeTagEnum {
  SUCCESS = 'success',
  PROCESSING = 'processing',
  ERROR = 'error',
  WARNING = 'warning',
  WAITING = 'waiting',
  STOP = 'stop',
}
export enum StatusEnum {
  ACTIVE = 1,
  INACTIVE = 0,
}

export enum TypeGroupFaq {
  GeneralInfo = 0,
  BuySim = 1,
  ActivateSim = 2,
}
